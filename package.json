{
  "name": "package.json",
  "version": "1.0.0",
  "description": "test Adeo command line",
  "main": "app.js",
  "scripts": {
    "test": "jest --config ./jest.config.json",
    "test:watch": "npm run test -- --watch"
  },
  "author": "clamarque",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
